{
  "name": "caspianshift-website",
  "version": "2.0.0",
  "private": true,
  "description": "Caspian Shift marketing site + Funding Analysis tool. Vanilla HTML/CSS/JS frontend, Netlify Functions backend.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
