node_modules
.netlify
.env
.env.local
.env.production
.DS_Store
*.log

# Windows alternate data streams created when files are copied from Windows
# into WSL — pure metadata cruft, no value to track.
*:Zone.Identifier

# Auto-generated by Netlify CLI for the Edge Functions runtime. This project
# has no edge functions; the file gets regenerated locally on `netlify dev`
# and shouldn't be committed.
deno.lock
